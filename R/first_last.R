#first_last.R
#returns first and last detection of given fish in specified period
first_last <- function(
    data,
    start_date = NULL,
    end_date = NULL,
    resolution){

    require(dplyr)
    require(lubridate)


    }
